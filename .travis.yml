sudo: false
language: java
jdk: 
 - oraclejdk8
cache:
  directories:
  - $HOME/.m2
env:
  global:
  - DISPLAY=:99.0
  - MAVEN_OPTS="-Xmx4g"
before_install:
- sh -e /etc/init.d/xvfb start - sleep 10
script:
- cd releng/com.yakindu.solidity.releng
- mvn clean verify -P!tests
